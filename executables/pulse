#!/usr/bin/env ruby
# encoding: utf-8

require 'pulse'

Pulse.connect hostname: 'irc.dk.quakenet.org', nickname: 'pulse' do
  catch :connection_ready do
    @connection.transmit :JOIN, '#warsow.na'
  end

  catch :message do |user, channel, line|
    puts "#{user} says #{line}"
  end
end
